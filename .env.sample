# Example configuration for authenticated GitHub requests
# Copy this file to `.env` and fill in your token.
# The code will automatically use authentication if this is set.

# GitHub token used for authenticated requests
GITHUB_TOKEN=ghp_your_personal_access_token_here

# Notes:
# - Only "public_repo" scope is required for accessing public content to avoid rate limits.
# - Leave this unset to make unauthenticated requests.
